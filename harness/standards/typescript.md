# TypeScript standard

- Enable strict mode and do not use `any` as an escape hatch.
- Parse external data at runtime before trusting its type.
- Model states so invalid combinations are difficult to represent.
- Keep side effects behind interfaces where practical.
- Prefer small named functions and explicit return types at public boundaries.
- Preserve useful error context and never swallow rejected promises.
